{% extends 'base.html.twig' %}

{% block title %}Informations de {{ Client.name }}{% endblock %}

{% block body %}

    <h1>Information du client : </h1>

    {{ form_start(form) }}

    {{ form_end(form) }}

    <hr>

{#    <h3>Produits :</h3>#}
{#    <div class="row">#}
{#        {% for p in Client.products %}#}
{#        <div class="col-sm-6">#}
{#            <div class="card">#}
{#                <div class="card-body">#}
{#                    <h5 class="card-title">{{ p.name }}</h5>#}
{#                    <p class="card-text">{{ p.description }}</p>#}
{#                    <a href="{{ url('productShow', {'id': p.id}) }}" class="btn btn-primary">Fiche produit</a>#}
{#                </div>#}
{#            </div>#}
{#        </div>#}
{#        {% endfor %}#}
{#    </div>#}

    <a href="{{ url('clientDelete', {'id': Client.id}) }}" class="btn btn-danger">Supprimer le client</a>

{% endblock %}
